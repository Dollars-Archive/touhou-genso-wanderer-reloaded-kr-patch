# 이상한 환상향 TOD -RELOADED- 한국어 패치 설치 가이드

현재 기준 버전은 **v1.0**입니다.

이 문서는 **PC판과 Nintendo Switch판** 한국어 패치 설치 방법을 안내합니다.

> [!IMPORTANT]
> PC판과 Nintendo Switch판은 **서로 다른 배포 패키지**를 사용합니다.  
> [Releases](https://github.com/Dollars-Archive/touhou-genso-wanderer-reloaded-kr-patch/releases/latest)에서 사용하는 환경에 맞는 파일을 받아 주세요.

---

## 0. 지원 버전 확인

### PC판

- **Steam판 Touhou Genso Wanderer -Reloaded-**
- 패처가 지원하는 깨끗한 원본 파일에서만 설치할 수 있습니다.

> [!TIP]
> PC 패처에서 지원하지 않는 파일이라는 오류가 나오면 Steam의 **게임 파일 무결성 검사**로 원본을 복구한 뒤 다시 시도해 주세요.

### Nintendo Switch판

- Base Title ID: `01004E900B082000`
- 본편 버전: `v1.0.0`
- Update Title ID: `01004E900B082800`
- 지원 업데이트: **v1.0.3**
- 본편 MD5: `86EC70ACA6671AEB27767AF7DAA35071`
- 업데이트 MD5: `9C03837890CAA62FE021081BB2E02A5F`

> [!NOTE]
> DLC의 MD5 값은 한국어 패치 적용 조건에 포함되지 않습니다.

> [!WARNING]
> Switch판은 반드시 **지원 버전 v1.0.3이 반영된 깨끗한 원본**을 사용하세요.  
> 이미 다른 번역 패치나 모드가 적용된 파일은 사용하지 않는 것을 권장합니다.

---

## 1. 패치 다운로드

1. [Releases](https://github.com/Dollars-Archive/touhou-genso-wanderer-reloaded-kr-patch/releases/latest)를 엽니다.
2. **PC판** 또는 **Nintendo Switch판** 중 사용하는 환경에 맞는 패키지를 받습니다.
3. 받은 ZIP을 원하는 위치에 **완전히 압축 해제**합니다.

> [!CAUTION]
> ZIP 내부에서 설치 파일을 바로 실행하지 마세요.  
> 반드시 먼저 압축을 완전히 해제한 뒤 실행해 주세요.

---

## 2. PC판 설치

1. 실행 중인 게임과 관련 프로그램을 모두 종료합니다.
2. 압축을 해제한 패치 폴더에서 `PC_KR_설치.cmd`를 실행합니다.
3. 폴더 선택창에서 **Touhou Genso Wanderer -Reloaded- 최상위 게임 폴더**를 선택합니다.

예시 구조:

```text
Touhou Genso Wanderer -Reloaded-
├─ LotusLandVReloaded.exe
└─ data
```

<img width="1759" height="1554" alt="스크린샷 2026-09-25 004500" src="https://github.com/user-attachments/assets/3e76e281-edd6-4ba2-a84f-5aa3526bbdd0" />

4. 설치 중 표시되는 배경 이미지 옵션에서 원하는 항목을 선택합니다.
5. 원본 검사 → 패치 적용 → 결과 검사가 끝날 때까지 기다립니다.
6. 완료 후 게임을 실행해 한국어 적용 여부를 확인합니다.

설치기는 지원 원본과 패치 파일, 완성 결과의 **SHA-256을 검사**합니다.

### 설치 상태 검증

패치 폴더의 아래 파일을 실행합니다.

```text
PC_KR_검증.cmd
```

현재 설치된 한국어 패치 파일과 선택된 배경 설정을 다시 검사합니다.

### 원본으로 복구

아래 파일을 실행합니다.

```text
PC_KR_복구.cmd
```

검증된 원본 백업 또는 복구용 차분을 사용해 대상 파일을 깨끗한 원본 상태로 되돌립니다.

> [!TIP]
> 설치 과정에서 보존된 깨끗한 원본은 게임 폴더의 `_PC_KR_ORIGINAL_BACKUP_V2`에 저장될 수 있습니다.

---

## 3. Nintendo Switch판 패처 실행

1. Switch판 배포 ZIP을 원하는 위치에 **완전히 압축 해제**합니다.
2. `이상한환상향_TODR_KR_Patcher.exe`를 실행합니다.
3. 패처가 요구하는 **지원 버전의 깨끗한 원본 폴더**를 선택합니다.
4. 결과물을 만들 위치를 선택합니다.
5. 배경 이미지 옵션에서 원하는 항목을 선택합니다.
6. 패치 생성을 시작합니다.
7. 원본 검사 → 패치 적용 → 결과 검사가 끝날 때까지 기다립니다.

패처는 지원 원본과 차분 파일을 확인하고, 완성 결과의 **SHA-256 검증을 통과한 경우에만** Switch용 결과를 생성합니다.

생성 결과에서 실제 설치에 사용하는 것은 `atmosphere` 폴더입니다.

최종 적용 구조:

```text
atmosphere
└─ contents
   └─ 01004E900B082000
      └─ romfs
         └─ ...
```

---

## 4. Nintendo Switch 적용 방법

> [!CAUTION]
> **기존 한국어 패치 위에 그대로 덮어쓰지 마세요.**  
> 오래된 파일이 남아 있으면 새 패치와 충돌할 수 있습니다.

### 방법 A. SD 카드를 PC에 연결해서 설치

1. 실행 중인 게임을 완전히 종료합니다.
2. SD 카드를 분리해서 작업할 경우 Switch 전원을 끈 뒤 SD 카드를 PC에 연결합니다.
3. 아래 기존 모드 폴더가 있다면 **백업 후 삭제하거나 내부를 완전히 비웁니다.**

```text
SD:/atmosphere/contents/01004E900B082000
```

4. 패처가 생성한 결과물 안의 `atmosphere` 폴더를 SD 카드 **최상위 루트**에 복사합니다.
5. 복사가 끝난 뒤 아래 경로를 확인합니다.

```text
SD:/atmosphere/contents/01004E900B082000/romfs/...
```

6. SD 카드를 Switch에 다시 넣고 부팅한 뒤 게임을 실행해 한국어 적용 여부를 확인합니다.

### 방법 B. DBI MTP로 SD 카드 분리 없이 설치

1. 실행 중인 게임을 완전히 종료합니다.
2. Switch에서 **DBI**를 실행합니다.
3. **`Run MTP responder`**를 실행합니다.
4. 데이터 전송이 가능한 USB 케이블로 Switch와 PC를 연결합니다.
5. PC 파일 탐색기에서 **`1: SD Card`**를 엽니다.
6. 아래 기존 모드 폴더가 있다면 백업 후 삭제하거나 내부를 완전히 비웁니다.

```text
1: SD Card/atmosphere/contents/01004E900B082000
```

7. 패처가 생성한 결과물 안의 `atmosphere` 폴더를 **`1: SD Card` 최상위 루트**에 복사합니다.
8. 복사가 끝나면 MTP responder를 종료하고 게임을 실행합니다.

> [!TIP]
> 정상적으로 적용되면 별도 재부팅이 필요하지 않을 수 있습니다.  
> 한국어 패치가 적용되지 않을 때만 본체를 재부팅한 뒤 다시 확인해 보세요.

> [!WARNING]
> `01004E900B082000` 폴더를 SD 카드 루트에 바로 넣는 것이 아닙니다.  
> 반드시 `SD:/atmosphere/contents/01004E900B082000/` 구조가 되어야 합니다.

---

## 5. 배경 이미지 옵션

PC판과 Nintendo Switch판 모두 패치 적용 중 **대체 배경 사용** 또는 **원본 게임 배경 유지**를 선택할 수 있습니다.

<img width="1215" height="641" alt="스크린샷 2026-09-25 004516" src="https://github.com/user-attachments/assets/1d282dcf-fcfb-44c6-a810-e8d0469c5939" />

<img width="1920" height="740" alt="배경 이미지 변경 전후 비교" src="https://github.com/user-attachments/assets/6c8bb586-2061-43b2-b0aa-45d6d8dec1c3" />

두 옵션은 해당 연출의 배경 이미지와 회전 효과만 다르며, **번역 내용과 게임 진행에는 차이가 없습니다.**

선택한 배경은 나중에 패처를 다시 실행해 변경할 수 있습니다.

---

## 6. 패치가 적용되지 않을 때 확인

### PC판

1. `Touhou Genso Wanderer -Reloaded-` **최상위 게임 폴더**를 선택했는지 확인
2. 게임이나 관련 프로그램이 실행 중이지 않은지 확인
3. 다른 패치 또는 임의 수정 파일이 섞여 있지 않은지 확인
4. Steam 게임 파일 무결성 검사 후 다시 시도
5. `PC_KR_검증.cmd`로 현재 설치 상태 확인

### Nintendo Switch판

1. 게임이 **지원 버전 v1.0.3**인지 확인
2. Base Title ID가 `01004E900B082000`인지 확인
3. 지원 업데이트가 반영된 깨끗한 원본인지 확인
4. 기존 `SD:/atmosphere/contents/01004E900B082000` 폴더를 먼저 정리했는지 확인
5. 결과물의 `atmosphere` 폴더를 SD 카드 루트에 복사했는지 확인
6. 게임을 완전히 종료한 뒤 다시 실행하고, 필요할 때만 본체를 재부팅해 확인

---

## 7. v1.0 주요 한국어화 범위

- 대사 / 이벤트 / 메뉴 / UI
- 아이템 / 스킬 / 도감
- 캐릭터명, 기술명, 던전·지역·시설·NPC 명칭 최신 검수본
- 한글 폰트
- 동영상 자막 데이터
- 한글화 영상 및 튜토리얼 / 이벤트 이미지
- 일부 용어 및 줄바꿈 전수 보정
- 설치 시 대체 배경 / 원본 게임 배경 선택 지원

---

## 지원 환경

- **PC:** Steam판
- **Nintendo Switch:** v1.0.3

---

## 주의사항

- 본인이 소유한 게임에서 준비한 원본을 사용하세요.
- 이미 다른 번역 패치나 모드가 적용된 파일은 사용하지 않는 것을 권장합니다.
- 패처가 지원하지 않는 원본을 억지로 수정해서 적용하지 마세요.
- 본 패치는 팬 제작 비공식 한국어 패치입니다.
- 배포물에는 게임 본편 및 완성된 원본 게임 파일이 포함되지 않습니다.
